
/**
 * PostCSS configuration.
 *
 * @type {import('postcss').ProcessOptions}
 *
 * @property {Object} plugins - The list of PostCSS plugins.
 * @property {Object} plugins.tailwindcss - Tailwind CSS plugin configuration.
 * @property {Object} plugins.autoprefixer - Autoprefixer plugin configuration.
 */
export default /** @type {import('postcss').ProcessOptions} */ ({
  plugins: {
    /** Tailwind CSS plugin */
    tailwindcss: /** @type {object} */ ({}),

    /** Autoprefixer plugin */
    autoprefixer: /** @type {object} */ ({}),
  },
});
/**
 * PostCSS configuration.
 *
 * @type {import('postcss').ProcessOptions}
 *
 * @property {Object} plugins - The list of PostCSS plugins.
 * @property {Object} plugins.tailwindcss - Tailwind CSS plugin configuration.
 * @property {Object} plugins.autoprefixer - Autoprefixer plugin configuration.
 */
export default /** @type {import('postcss').ProcessOptions} */ ({
  plugins: {
    /** Tailwind CSS plugin */
    tailwindcss: /** @type {object} */ ({}),

    /** Autoprefixer plugin */
    autoprefixer: /** @type {object} */ ({}),
  },
});
  plugins: {
    tailwindcss: {},
    autoprefixer: {},
  },
}
